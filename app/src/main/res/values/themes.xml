<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PasswordManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/purple_200</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/blue</item>
        <item name="background">@color/DimGrey</item>
    </style>

    <style name="Base.Theme.PasswordManager.headers">
        <item name="fontFamily">@font/roboto_medium</item>
        <item name="android:textSize">40sp</item>
    </style>
    <style name="Theme.PasswordManager" parent="Base.Theme.PasswordManager" />

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/purple_500</item>
    </style>

</resources>